import {useState} from "react";

let menu = document.getElementsByClassName("menuInterface");

export function manipulateMenu(){
    const [menuSize, setMenuSize] = useState(300);
    // menu.style.width=menuSize;
}